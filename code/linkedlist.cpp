#include <iostream>

using namespace std;

struct Node {
	int data;
	Node *next;
};

void insert(Node* n, Node* head) {


}

int main() {

	// ll is a node pointer
	Node *ll;
	// ll is now pointing to a new node which ha sbeen
	// created
	ll = new Node;
	ll->data = 2;
	ll->next = NULL;
	return 0;
}
